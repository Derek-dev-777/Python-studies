# dir, hasattr e getattr

#hasattr checa se algo tem algum atributo
#getattr mostra O atributo que o objeto tem
hastest = print(" Derek ")
print(hastest.upper())


if hasattr(hasattr, "upper"):
    print("existe upper")